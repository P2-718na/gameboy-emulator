ADD_EXECUTABLE(gameboy main.cpp)

ADD_SUBDIRECTORY(Engine)
ADD_SUBDIRECTORY(Config)

TARGET_LINK_LIBRARIES(gameboy Engine)

SET_TARGET_PROPERTIES(
        gameboy PROPERTIES
        RUNTIME_OUTPUT_DIRECTORY ${PROJECT_BINARY_DIR})
